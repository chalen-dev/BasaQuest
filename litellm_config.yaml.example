model_list:
  - model_name: gemini-3.5-flash-lite
    litellm_params:
      model: gemini/gemini-3.5-flash-lite
      api_key: <insert your gemini free api key here>

# run command
# litellm --config litellm_config.yaml --port 4000

# sample prompt (copy-paste)
# make a commit message, scrap those old changes files you keep using one, find the very new changes in the commit window MAKE IT WHERE IT ENCOMPASSES ALL CHANGES NOT JUST A LITTLE, SINGLE PART